# Backend Tasks

## Immediate

- Create a human-facing backend owner manual that explains current backend state, data, ingestion, staging, canonical data, safe Codex changes, human approval gates, next milestone, and verification. Done: see `BACKEND_OWNER_MANUAL.md`.
- Define an exhibition-first JSON schema for canonical records and staged review items. Done: see `schemas/exhibition.schema.json` and `schemas/exhibition-staging.schema.json`.
- Add fixture-based tests for the current ingestion diff behavior so staging remains stable while parsers change. Done: see `scripts/exhibit-ingest/ingest.test.mjs`.
- Add an exhibition source adapter for one official NYC venue page, using staging output only. Done: see New Museum parser/source config and `data/staging/new-museum-exhibitions.json`.
- Add David Zwirner as the next official NYC gallery exhibition source, treating the older artist/artwork parser as legacy context only. Done in staging-only form with a dedicated exhibition parser, official live source config, offline fixture-backed verification config, and review dry-run command.
- Rename or document `data/exhibit-records.json` clearly as the canonical JSON store until a database is introduced. Done: see `data/README.md`.
- Add dedupe notes to staging reports for candidates with matching normalized title, venue, date, or source URL. Done: staged exhibition creates now run conservative duplicate scoring and become `possibleDuplicate` when a canonical match is likely or plausible.
- Add schema validation to tests once a JSON Schema validator dependency is selected. Done: staging reports are validated with AJV 2020-12 during ingestion and in tests.
- Add source-config validation so malformed source metadata fails before fetch/parse work starts. Done: see `schemas/exhibition-source.schema.json`, `scripts/exhibit-ingest/schema-validation.mjs`, and `scripts/exhibit-ingest/schema-validation.test.mjs`.

## Next

- Add a reviewer-friendly staging format that separates creates, updates, possible duplicates, and conflicts. Done for the current JSON workflow: staging reports include `items` with `proposalType`, reviewer summary/JSON output groups those sections for human audit, and high-risk exhibition disagreements now stage as `conflict` items instead of ordinary updates.
- Add aggregate reviewer insights for multi-location sources so humans can quickly audit venue coverage and current/upcoming splits. Done for the current JSON workflow: reviewer summary and JSON output now include venue counts plus `current`/`upcoming` status-tag counts.
- Add reviewer readiness signals so humans can quickly tell whether a staged source meets the minimum exhibition contract before approval work starts. Done for the current JSON workflow: reviewer summary and JSON output now include minimum-field coverage for `title`, `venue`, `startDate`, and `sourceUrl`, plus explicit recommended-coverage warnings for missing fields such as `description` and `imageUrl`.
- Add reviewer-visible readiness/blocker output so a staged source makes its live-verification state and next actions explicit before human review starts. Done for the current JSON workflow: reviewer summary and JSON output now include readiness status, blockers, warnings, and next actions derived from verification state plus staged field coverage.
- Add reviewer-visible completeness signals so staged exhibition sources make missing metadata obvious before human approval. Done for the current JSON workflow: reviewer summary/JSON output now include field-coverage totals plus per-item missing-field notes for review-relevant exhibition metadata.
- Surface source scope in staged summaries and reviewer output so humans can see exactly which official slice is under review, including intentional exclusions such as Whitney `online` and `public art`. Done: staging summaries now carry source-config `stagingNotes`, and reviewer summary/JSON output expose that scope directly.
- Surface source verification state in staged summaries so reviewers can distinguish fixture-backed offline checks from live-verified official source runs. Done: staging summaries now include `summary.verification`, and reviewer output prints the current live-verification status and notes.
- Track source reliability and source type on every staged claim. Done for newly generated staging items; keep this requirement for future adapters.
- Add a simple approval script that can promote reviewed staged exhibition creates into the canonical JSON store. Done for approved `create` items with dry-run default; CLI coverage now exists; next step is reviewer workflow documentation.
- Run the live David Zwirner exhibition staging command in a network-enabled environment and compare the output against the fixture-backed sample to confirm the parser still matches the official page structure. Done: the live artifact now matches the fixture-backed artifact with `added=0 removed=0 changed=0`.
- After the live David Zwirner verification run succeeds, update `scripts/exhibit-ingest/sources/david-zwirner-exhibitions*.json` from `pending_live_verification` to `verified_live` with the actual verification timestamp. Done: both David Zwirner exhibition source configs are now `verified_live` at `2026-06-23T15:00:00.000Z`.
- Preserve fixture-backed staging artifacts during live verification runs. Done: `scripts/exhibit-ingest/ingest.mjs` now accepts `--output`, so source checks can write a separate live staging file instead of overwriting the default review artifact.
- Add a staging comparison CLI for live-vs-fixture source verification. Done: `scripts/exhibit-ingest/compare-staging.mjs` validates two staging reports and reports added, removed, and changed staged records.
- Surface verification-state changes in live-vs-fixture staging comparisons so reviewers can confirm when a source moved from fixture-backed to live-verified. Done: `compare-staging.mjs` now includes verification metadata in both text and JSON output, with test coverage.
- Add a dry-run-first live verification helper that only updates source verification metadata when a live staging artifact matches the fixture-backed review artifact. Done: `scripts/exhibit-ingest/verify-source-live.mjs` checks live-vs-fixture parity and requires `--apply` before mutating source configs.
- Keep source-specific staging/review commands sequential in automation runs; parallel execution can race on the same staging file and cause misleading validation failures.
- Add machine-readable JSON output to the reviewer summary CLI. Done: `scripts/exhibit-ingest/review-staging.mjs` now supports `--json`, with source-specific package scripts for David Zwirner and New Museum.
- Add a safe official-source path for richer David Zwirner exhibition metadata such as descriptions and images when the official site exposes them, while keeping the flow staging-only. Done: the parser now merges optional page-level JSON-LD `description` and `image` metadata into staged exhibition records when the official source exposes them, with fixture-backed test coverage.
- David Zwirner backend flow is now end to end for staging and review: keep it staging-only and wait for human approval before any canonical promotion.
- Start the next official NYC source with Whitney Museum exhibitions. Current staging slice done: fixture index snapshot, fixture-backed detail-page snapshots, live/fixture source configs, staging-only parser, staged review artifact, source-specific review commands, and live-vs-fixture parity check for the official index-plus-detail flow.
- Whitney next gap: clearer reviewer messaging for the remaining `description` gap on `Roy Lichtenstein`. Done: reviewer summary and JSON output now surface item-level recommended-gap details, including the exact affected Whitney record and official source URL.
- Whitney follow-up: align staged `sourceNotes` with the current index-plus-detail flow so reviewers are not told that detail-page parsing is still pending. Done: Whitney staged records now describe `dateText` as preserved list-page context while linked official detail pages provide exact dates and richer metadata.
- Whitney remaining decision: decide whether `online` and `public art` need separate source treatment or continued exclusion, now that the in-person current/upcoming slice is live-verified and ready for human review.
- Start the next official NYC source after Whitney with ICP exhibitions. Done for the first staging-only slice: fixture snapshot, fixture/live source configs, parser, staged review artifact, source-specific review commands, planned-source notes, and live-vs-fixture parity check for the official current/upcoming card flow.
- ICP detail-page enrichment is already active in the current staging slice, and the checked-in reviewer inbox now carries `description`/`imageUrl` coverage from linked official exhibition pages while remaining `verified_live`.
- ICP follow-up: keep the verified-live source staging-only, preserve the new summary-only description boundaries so reviewer text stays focused on exhibition descriptions instead of biography, featured-list, or newsletter content, and treat the current checked-in fixture/live parity contract as 3 current exhibitions from 1 configured index page plus 3 followed detail pages.
- Add reviewer-visible page-coverage output for multi-page sources so humans can distinguish configured listing pages from followed official detail pages during audit. Done: staging reports now label each fetched page as `configured` or `followed`, and reviewer summary/JSON output expose configured/followed plus fixture/live counts.
- Protect fixture-backed followed-page sources from silently falling back to live detail-page fetches during offline verification. Done: fixture-backed source configs that set `followRecordUrls: true` must now also declare `followRecordUrlFixtureDirectory`, with schema-validation coverage.
- ICP follow-up after detail-page enrichment: keep the current source staging-only and decide whether the separate `future-exhibitions` landing page needs its own source or continued exclusion before adding any further section expansion.
- Start the next official NYC source after ICP with The Jewish Museum exhibitions. Done for the first staging-only slice: planned-source notes, fixture snapshot, fixture/live source configs, parser registration, source-specific package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the visible server-rendered `on view` plus `coming soon` cards before the client-side schedule calendar.
- Jewish Museum follow-up: keep the verified-live card-only source staging-only; the parser, fixture/live source configs, source-specific review commands, and source-specific live-verification script now all exist in code, and the current reviewer inbox remains minimum-ready but still has `description=0/8`, so decide whether linked official detail pages should be added for richer optional metadata or whether the client-side schedule calendar belongs in its own source-specific slice.
- Start the next official NYC source after The Jewish Museum with Guggenheim exhibitions. Done for the first staging-and-review slice: fixture snapshot, bootstrap-payload parser, fixture/live source configs, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official `on_view` plus `upcoming` index-page payload.
- Guggenheim follow-up: keep the verified-live bootstrap source staging-only; decide whether reviewer-visible warnings are needed for long-running or stale on-view entries like `Thannhauser Collection`, and separately decide whether official detail-page enrichment is worth adding.
- Start the next official NYC source after Guggenheim with The Drawing Center exhibitions. Done for the first staging-and-review slice: fixture snapshot, server-rendered listing parser, fixture/live source configs, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official `onview` plus `upcoming` listing modules.
- Drawing Center follow-up: keep the verified-live listing source staging-only; the reviewer inbox is now minimum-ready from index-page data alone, and the remaining optional metadata gaps are `description=0/3` and `imageUrl=2/3`, so decide separately whether detail-page enrichment is worth adding.
- Start the next official NYC source after The Drawing Center with Museum of Arts and Design exhibitions. Discovery slice done: documented the official exhibitions page, its fetchability, the visible server-rendered `Current Exhibitions` and `Upcoming` grids, and the smallest safe parser target in `scripts/exhibit-ingest/sources/planned/mad-exhibitions.md`.
- Start the next official NYC source after The Drawing Center with Museum of Arts and Design exhibitions. Done for the first staging-and-review slice: fixture snapshot, current/upcoming listing parser, fixture/live source configs, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official listing-page flow.
- MAD follow-up: official detail-page enrichment is now active in the verified-live staging slice, and the reviewer inbox now carries `description=6/6`, `endDate=5/6`, plus full source-page coverage for 1 configured index page and 6 followed official detail pages.
- MAD remaining blocker: the current source is still not minimum-ready because only 1 of 6 staged records exposes an exact `startDate`; decide whether another official-source path can recover those 5 current start dates or whether the minimum review contract should be reconsidered for already-open exhibitions with trusted `endDate` coverage.
- Start the next official NYC source after MAD with The Frick Collection exhibitions. Done for the first staging-and-review slice: fixture/live source configs, current/upcoming parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official listing-page flow.
- Frick follow-up: keep the verified-live listing source staging-only; the reviewer inbox is already minimum-ready from visible card data alone, and the next separate decision is whether any official detail-page enrichment would add enough value to justify expanding beyond the current list-page slice.
- Start the next official NYC source after Frick with The Noguchi Museum exhibitions. Done for the first staging-and-review slice: fixture/live source configs, museum-only listing parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official `Current & Upcoming` listing slice before the first explicit `offsite:` entry.
- Noguchi follow-up: keep the verified-live museum-only source staging-only; the reviewer inbox is already minimum-ready from list-page data alone, and the next separate decision is whether official detail-page enrichment is worth adding for optional `description` coverage.
- Start the next official NYC source after Noguchi with The Morgan Library & Museum exhibitions. Done for the first staging-and-review slice: fixture/live source configs, a two-page current/upcoming listing parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official listing-page flow while excluding the current-page `Collection Spotlight` block.
- Morgan follow-up: keep the verified-live current/upcoming listing source staging-only; the reviewer inbox now stages 9 creates from 2 configured official pages, and the remaining blocker is explicit at `startDate=8/9` because `J. Pierpont Morgan's Library` is still `Ongoing` on the official page.
- Start the next official NYC source after Morgan with Museum of the City of New York exhibitions. Done for the first staging-and-review slice: fixture/live source configs, current/upcoming card parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official listing-page flow while excluding online and traveling sections.
- MCNY follow-up: keep the verified-live listing source staging-only; the reviewer inbox now stages 11 creates from the visible `on view` plus `upcoming` cards, and the remaining blocker is explicit at `startDate=2/11` because most current cards expose only `Through ...` or `Ongoing` date text on the official page.
- Start the next official NYC source after MCNY with Museum at FIT exhibitions. Done for the first staging-and-review slice: fixture/live source configs, linked-card long-card parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official current/upcoming linked-card flow.
- FIT follow-up: keep the verified-live linked-card source staging-only; the reviewer inbox still stages 1 pending create from the official detail-linked slice only after the latest live-parity refresh plus description cleanup, and the next separate decision is whether current long cards without official exhibition pages should remain excluded or be modeled in a different source-specific way.
- Start the next official NYC source after FIT with Bronx Museum exhibitions. Done for the first staging-and-review slice: fixture/live source configs, current-grid parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official current exhibition-card grid while excluding the separate featured-show hero duplicate.
- Bronx Museum follow-up: keep the verified-live current-grid source staging-only; the reviewer inbox now stages 2 creates from 1 configured official page, remains minimum-ready, and keeps the current first-slice scope explicit while the separate decisions on youth-program cards, `Upcoming`, `Archive`, and detail-page enrichment stay deferred.
- Start the next official NYC source after Bronx Museum with Cooper Hewitt exhibitions. Done for the first staging-and-review slice: fixture/live source configs, a dedicated current/upcoming parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the visible main exhibition blocks plus `Learn more` links on the official WordPress pages.
- Cooper Hewitt follow-up: keep the verified-live current/upcoming listing source staging-only; the source configs were refreshed again at `2026-06-26T13:06:33.076Z`, the reviewer inbox now stages 4 creates from 2 configured official pages, carries `description=4/4` and `imageUrl=4/4`, and keeps the remaining blocker explicit at `startDate=1/4` because the 3 current exhibitions publish only `On view through ...` text on the official listing pages.
- Start the next official NYC source after Cooper Hewitt with Poster House exhibitions. Done for the first staging-and-review slice: fixture/live source configs, a dedicated current/upcoming parser, source-specific staging/review/live-verification package scripts, checked-in staging artifacts, and live-vs-fixture parity validation for the official exhibitions page flow that combines visible cards with the embedded exhibition payload.
- Poster House follow-up: keep the verified-live current/upcoming source staging-only; the reviewer inbox now stages 10 creates from 1 configured official page, is `ready_for_human_review`, and keeps `Past Exhibitions`, the archive link, poster-loan content, and detail-page enrichment out of scope for separate later decisions.
- Start the next official NYC source after Poster House with Neue Galerie exhibitions. Discovery-plus-fixture slice already exists: `scripts/exhibit-ingest/sources/planned/neue-galerie-exhibitions.md` documents the official exhibitions route, the checked-in fixture at `scripts/exhibit-ingest/fixtures/neue-galerie-exhibitions.html`, and the initial Next.js payload boundary.
- Neue Galerie next slice: inspect the embedded Next.js payload for the narrowest exhibition-only listing data, then add fixture/live source configs only after that boundary is clear enough for a conservative staging-only parser.
- Decide whether Google Sheets is an active external review surface for this project; if yes, document the tab names and sync direction.
- Draft a PostgreSQL migration plan for canonical exhibitions, venues, sources, staged proposals, and change history.
- Add conflict detection for high-confidence source matches that disagree on meaningful fields such as date, venue, title, or exhibition URL. Initial slice done: exhibition updates that change reviewer-sensitive fields now stage as `conflict` items with canonical/proposed values and reviewer notes; future work can refine thresholds and expand field-level provenance.
- Add tests for the approval script CLI using temporary files and `--apply`. Done: see `scripts/exhibit-ingest/approve-staging.test.mjs`.
- Address or explicitly defer the 5 npm audit findings surfaced after adding AJV.

## Later

- Add venue normalization for NYC museums and galleries.
- Add field-level provenance for dates, descriptions, images, artists, and source URLs.
- Add change history exports before bulk approvals or source-wide refreshes.
- Add an API contract for future browse/search/filter/save/share frontend workflows.
- Revisit artist entities after the exhibition and venue workflow is healthy.

## Open Decisions

- Whether staging should remain file-based, move to Google Sheets, move to database tables, or use a hybrid during transition.
- Whether ICP's separate `future-exhibitions` landing page should remain excluded until it exposes stable listing cards or be modeled as a separate source.
- Whether The Jewish Museum detail pages should be followed for optional `description` enrichment or whether the current verified index-only reviewer inbox is sufficient.
- Whether Whitney's compact list-page date strings should remain staged as reviewer-facing `dateText` now that the linked official detail pages provide exact `startDate` and `endDate`.
- How strict the first dedupe threshold should be for same-title exhibitions across different venues or dates.
- Whether canonical IDs should be URL-derived, venue-date-title-derived, or generated with explicit aliases.
