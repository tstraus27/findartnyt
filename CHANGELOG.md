# Changelog

## 2026-06-26 Poster House Source Completion And Neue Galerie Queue Handoff

- Finished the first Poster House backend slice end to end instead of leaving it at discovery-only status: the checked-in parser/tests, fixture/live source configs, source-specific staging/review/live-verification scripts, and staged reviewer artifacts now align and verify cleanly.
- Kept the Poster House scope conservative at the official exhibitions page only, using the visible `On View` and `Upcoming Exhibitions` cards for exact date strings, URLs, and images, then enriching those same records with the page's embedded exhibition payload for descriptions while excluding `Past Exhibitions`, the archive link, poster-loan content, and any detail-page enrichment.
- Generated `data/staging/poster-house-exhibitions.json` and refreshed `data/staging/poster-house-exhibitions.live.json`; the current Poster House reviewer inbox stages 10 pending creates from 1 configured official page, is now `verified_live`, and is already `ready_for_human_review` with `description=10/10`, `imageUrl=10/10`, and full minimum-field coverage.
- Confirmed `npm run compare:staging -- --baseline data/staging/poster-house-exhibitions.json --candidate data/staging/poster-house-exhibitions.live.json` reports `added=0 removed=0 changed=0`, re-ran the live staging command successfully against the official Poster House exhibitions route on 2026-06-26, and confirmed `npm run verify:source:poster-house:live -- --verified-at 2026-06-26T19:10:45.280Z` remains eligible with zero blockers or warnings without touching canonical records.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, `EXHIBITION_BACKEND_BRIEF.md`, and `scripts/exhibit-ingest/sources/planned/neue-galerie-exhibitions.md` so the repo now reflects Poster House as another completed verified-live staging source while moving the next-source queue forward to Neue Galerie.
- Re-checked official next-source candidates live on 2026-06-26 and kept Neue Galerie as the next focused target because `https://www.neuegalerie.org/exhibitions` still returned `HTTP 200` here while El Museo returned `HTTP 403` bot protection and New-York Historical returned an AWS WAF `202` challenge; the checked-in Neue Galerie fixture still looks like the safest next discovery handoff even though the payload is much larger than Poster House.
- Verified `node --test scripts/exhibit-ingest/parsers/poster-house-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/source-config-staging-alignment.test.mjs scripts/exhibit-ingest/staging-artifacts.test.mjs`, `npm run ingest:exhibits:poster-house:stage`, `npm run review:staging:poster-house:summary`, `npm run review:staging:poster-house:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/poster-house-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/poster-house-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/poster-house-exhibitions.json --candidate data/staging/poster-house-exhibitions.live.json`, `npm run verify:source:poster-house:live -- --verified-at 2026-06-26T19:10:45.280Z`, `npm test`, `npm run build`, and live fetch checks for Neue Galerie, El Museo del Barrio, and New-York Historical.
- Remaining next-source work: keep Poster House staging-only, then isolate the narrowest exhibition-only structure inside Neue Galerie's Next.js payload before adding source configs or a parser.

## 2026-06-26 Cooper Hewitt Source Completion And Poster House Discovery Slice

- Finished the first Cooper Hewitt backend slice end to end instead of leaving it at discovery-only status: the checked-in parser/tests, fixture/live source configs, source-specific staging/review/live-verification scripts, and staged reviewer artifacts now align and verify cleanly.
- Kept the Cooper Hewitt scope conservative at the official current and upcoming exhibition pages only, using the visible main exhibition blocks plus `Learn more` links while excluding sponsor-logo sections, funding acknowledgements after the main exhibition copy, photo-credit blocks, previous/traveling/digital pages, and any detail-page enrichment.
- Generated `data/staging/cooper-hewitt-exhibitions.json` and refreshed `data/staging/cooper-hewitt-exhibitions.live.json`; the current Cooper Hewitt reviewer inbox stages 4 pending creates from 2 configured official pages, remains `verified_live`, carries `description=4/4` and `imageUrl=4/4`, and keeps the remaining blocker explicit at `startDate=1/4` because the 3 current listings publish only `On view through ...` text.
- Fixed the upcoming-page hero-image capture so the official `Design Across Time` record now keeps `imageUrl` populated when the source markup exposes only the page hero image rather than an inline body image.
- Confirmed `npm run compare:staging -- --baseline data/staging/cooper-hewitt-exhibitions.json --candidate data/staging/cooper-hewitt-exhibitions.live.json` reports `added=0 removed=0 changed=0`, re-ran the live staging command successfully against both official Cooper Hewitt routes on 2026-06-26, and refreshed both Cooper Hewitt source configs plus the fixture-backed reviewer artifact to `verified_live` at `2026-06-26T13:06:33.076Z` without touching canonical records.
- Captured `scripts/exhibit-ingest/fixtures/poster-house-exhibitions.html` and added `scripts/exhibit-ingest/sources/planned/poster-house-exhibitions.md`, documenting Poster House as the next official NYC source because its official exhibitions page returned `HTTP 200` on 2026-06-26 and exposes a compact embedded exhibition payload with 4 official exhibition records, while New-York Historical returned an AWS WAF challenge and Neue Galerie remained fetchable but much heavier.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects Cooper Hewitt as another completed verified-live staging source while moving the next-source queue forward to Poster House.
- Verified `node --test scripts/exhibit-ingest/parsers/cooper-hewitt-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/source-config-staging-alignment.test.mjs scripts/exhibit-ingest/staging-artifacts.test.mjs`, `npm run ingest:exhibits:cooper-hewitt:stage`, `npm run review:staging:cooper-hewitt:summary`, `npm run review:staging:cooper-hewitt:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/cooper-hewitt-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/cooper-hewitt-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/cooper-hewitt-exhibitions.json --candidate data/staging/cooper-hewitt-exhibitions.live.json`, `npm run verify:source:cooper-hewitt:live -- --verified-at 2026-06-26T13:06:33.076Z`, the same command with `--apply`, `npm test`, `npm run build`, and live fetch checks for Poster House, Neue Galerie, and New-York Historical.
- Remaining next-source work: keep Cooper Hewitt staging-only, then add Poster House fixture/live source configs plus the first staging-only parser for the embedded official exhibition payload inside `.panel-exhibitions-container`.

## 2026-06-26 Bronx Re-Verification And Cooper Fixture Alignment Slice

- Re-ran the Bronx Museum staging flow sequentially, confirmed the fixture-backed and live artifacts still match with `added=0 removed=0 changed=0`, and refreshed both Bronx Museum source configs plus the fixture-backed reviewer artifact to `verified_live` at `2026-06-26T04:03:30.000Z`.
- Verified the Bronx Museum reviewer inbox still stages 2 pending creates from the visible `Current` archive grid only, stays `ready_for_human_review` on the minimum field contract, and keeps the optional `description=0/2` gap explicit without promoting anything into canonical data.
- Aligned the Cooper Hewitt discovery handoff with the checked-in fixture files by capturing `scripts/exhibit-ingest/fixtures/cooper-hewitt-exhibitions-upcoming.html`, copying the current-page capture to `scripts/exhibit-ingest/fixtures/cooper-hewitt-exhibitions-current.html`, and updating the planning docs so the next run can move directly into parser/config work.
- Updated `BACKEND_TASKS.md`, `EXHIBITION_BACKEND_BRIEF.md`, and `scripts/exhibit-ingest/sources/planned/cooper-hewitt-exhibitions.md` so the queue now treats Bronx Museum as complete and Cooper Hewitt as the active next source with the exact fixture filenames already in place.
- Verified `node --test scripts/exhibit-ingest/parsers/bronx-museum-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:bronx-museum:stage`, `npm run review:staging:bronx-museum:summary`, `npm run review:staging:bronx-museum:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/bronx-museum-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/bronx-museum-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/bronx-museum-exhibitions.json --candidate data/staging/bronx-museum-exhibitions.live.json`, `npm run verify:source:bronx-museum:live -- --verified-at 2026-06-26T04:03:30.000Z`, the same command with `--apply`, and live fetch checks for Cooper Hewitt, Neue Galerie, Poster House, El Museo del Barrio, and New-York Historical.
- Remaining next-source work: add Cooper Hewitt fixture/live source configs, build the first parser for the visible current and upcoming exhibition blocks plus `Learn more` links, and decide whether sponsor-logo/photo-credit tails plus repeated exhibition detail across the two routes should be deduped in the first reviewer inbox.

## 2026-06-26 Bronx Museum Source Completion Slice

- Added `scripts/exhibit-ingest/parsers/bronx-museum-exhibitions.mjs` and `scripts/exhibit-ingest/parsers/bronx-museum-exhibitions.test.mjs`, so The Bronx Museum now has a dedicated exhibition parser for the official exhibitions archive that keeps only the visible `Current` exhibition-card grid and excludes the separate featured-show hero duplicate plus the `Upcoming` and `Archive` filters.
- Added fixture/live Bronx Museum source configs plus source-specific staging, review, and live-verification package scripts, registered the parser in `scripts/exhibit-ingest/ingest.mjs`, extended source-config validation coverage, and captured `scripts/exhibit-ingest/fixtures/bronx-museum-exhibitions.html` so Bronx Museum plugs into the same staging-first backend contract as the existing official NYC sources.
- Generated `data/staging/bronx-museum-exhibitions.json` and `data/staging/bronx-museum-exhibitions.live.json`; the current Bronx Museum reviewer inbox stages 2 pending creates from 1 configured official page, is now `verified_live`, and is already `ready_for_human_review` on the minimum field contract while keeping the remaining optional `description=0/2` gap explicit.
- Confirmed `npm run compare:staging -- --baseline data/staging/bronx-museum-exhibitions.json --candidate data/staging/bronx-museum-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:bronx-museum:live -- --verified-at 2026-06-26T04:00:00.000Z --apply`, which marked both Bronx Museum source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects Bronx Museum as another completed verified-live staging source while keeping Cooper Hewitt as the next focused official NYC source from the existing discovery queue.
- Verified `node --test scripts/exhibit-ingest/parsers/bronx-museum-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:bronx-museum:stage`, `npm run review:staging:bronx-museum:summary`, `npm run review:staging:bronx-museum:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/bronx-museum-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/bronx-museum-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/bronx-museum-exhibitions.json --candidate data/staging/bronx-museum-exhibitions.live.json`, `npm run verify:source:bronx-museum:live -- --verified-at 2026-06-26T04:00:00.000Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining next-source work: keep Bronx Museum staging-only, then use the existing Cooper Hewitt discovery-and-fixture slice to add fixture/live source configs plus the first current/upcoming parser.

## 2026-06-26 Cooper Hewitt Discovery And Fixture Slice

- Kept the completed FIT source staging-only and unchanged, then advanced the next-source queue instead of widening the FIT parser beyond the current linked-card scope.
- Checked several official NYC exhibition pages live on 2026-06-26 and selected Cooper Hewitt as the safer next source candidate from this environment: both `https://www.cooperhewitt.org/exhibitions/` and `https://www.cooperhewitt.org/exhibitions/upcoming/` returned fetchable `HTTP 200` WordPress HTML with visible exhibition blocks and `Learn more` links, while MoMA returned a Cloudflare `403` challenge, Brooklyn Museum and the Met returned Vercel `429` challenge responses, and El Museo del Barrio still returned a browser-check page.
- Added `scripts/exhibit-ingest/sources/planned/cooper-hewitt-exhibitions.md` to document the official current/upcoming routes, the visible reviewer-useful fields, the first parser boundary, and why Cooper Hewitt is the tighter next target than nearby alternatives or the heavier Neue Galerie payload.
- Captured `scripts/exhibit-ingest/fixtures/cooper-hewitt-exhibitions-current.html` and `scripts/exhibit-ingest/fixtures/cooper-hewitt-exhibitions-upcoming.html` so the next Cooper Hewitt run can start from parser/config work instead of redoing fetch discovery.
- Updated `BACKEND_TASKS.md` and `EXHIBITION_BACKEND_BRIEF.md` so the queue now points to Cooper Hewitt as the active post-FIT source, with the next slice narrowed to fixture/live source configs plus a conservative parser for the visible exhibition blocks and `Learn more` links.
- Verified `npm test`, `npm run build`, `curl -I -L --max-time 20 -A 'Mozilla/5.0 exhibit-ingest/0.1' https://www.neuegalerie.org/exhibitions`, the same fetchability check for Cooper Hewitt, MoMA, Brooklyn Museum, and the Met, plus live HTML capture for the Cooper Hewitt current/upcoming routes.
- Remaining next-source work: add Cooper Hewitt fixture/live source configs, build the first parser for the visible current and upcoming exhibition blocks, and decide whether sponsor-logo/photo-credit tails plus repeated exhibition detail across the two routes should be deduped in the first reviewer inbox.

## 2026-06-26 FIT Source Completion And Bronx Museum Discovery Slice

- Finished the first Museum at FIT backend slice end to end instead of leaving it in discovery-only status: the checked-in FIT parser/tests, fixture/live source configs, source-specific staging/review/live-verification scripts, and staged reviewer artifacts now align and verify cleanly.
- Kept the FIT scope conservative at the official `Current` and `Upcoming` long-card sections only when a card exposes an official exhibition detail link, which excludes the `Galleries Closed` status card, excludes lobby-only cards without official exhibition pages, and leaves `MFIT on the Road`, `Past Exhibitions`, plus detail-page enrichment out of scope for this first staging-only slice.
- Refined the FIT parser output so reviewer-facing records now use the official venue name `The Museum at FIT`, carry the official mailing address suffix `10001-5992`, and drop the listing-page `Image:` caption tail from staged descriptions.
- Generated `data/staging/fit-exhibitions.json` and `data/staging/fit-exhibitions.live.json`; the current FIT reviewer inbox stages 1 pending create from 1 configured official page, remains `verified_live`, and is already `ready_for_human_review` on the minimum field contract.
- Confirmed `npm run compare:staging -- --baseline data/staging/fit-exhibitions.json --candidate data/staging/fit-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then re-applied `npm run verify:source:fit:live -- --verified-at 2026-06-25T18:45:00.000Z --apply`, which refreshed both FIT source configs plus the fixture-backed reviewer artifact metadata without touching canonical records.
- Added `scripts/exhibit-ingest/sources/planned/bronx-museum-exhibitions.md` to nominate Bronx Museum as the next official NYC source after FIT, based on the 2026-06-26 live fetch check and the visible server-rendered archive structure with explicit `Current`, `Upcoming`, and `Archive` filters plus exhibition-card markup.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects FIT as another completed verified-live staging source and moves the next-source queue forward to Bronx Museum.
- Verified `node --test scripts/exhibit-ingest/parsers/fit-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/source-config-staging-alignment.test.mjs scripts/exhibit-ingest/staging-artifacts.test.mjs`, `npm run ingest:exhibits:fit:stage`, `npm run review:staging:fit:summary`, `npm run review:staging:fit:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/fit-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/fit-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/fit-exhibitions.json --candidate data/staging/fit-exhibitions.live.json`, `npm run verify:source:fit:live -- --verified-at 2026-06-25T18:45:00.000Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining next-source work: capture a checked-in Bronx Museum exhibitions fixture, add fixture/live source configs, and build the first parser slice for the visible current exhibition cards while deciding whether featured-show duplication and youth-program exhibition cards belong in the first reviewer inbox.

## 2026-06-25 MCNY Source Completion And FIT Discovery Slice

- Added `scripts/exhibit-ingest/parsers/mcny-exhibitions.mjs` and `scripts/exhibit-ingest/parsers/mcny-exhibitions.test.mjs`, so Museum of the City of New York now has a dedicated exhibition parser for the official exhibitions page that keeps only the visible `Exhibitions On View` and `Upcoming Exhibitions` card sections and excludes `Online Exhibitions`, `Traveling Exhibitions`, `Past`, and detail-page enrichment.
- Added fixture/live MCNY source configs plus source-specific staging, review, and live-verification package scripts, registered the parser in `scripts/exhibit-ingest/ingest.mjs`, extended source-config validation coverage, and captured `scripts/exhibit-ingest/fixtures/mcny-exhibitions.html` so MCNY plugs into the same staging-first backend contract as the existing official NYC sources.
- Generated `data/staging/mcny-exhibitions.json` and `data/staging/mcny-exhibitions.live.json`; the current MCNY reviewer inbox stages 11 pending creates from 1 configured official page, is now `verified_live`, and keeps the remaining blocker explicit at `startDate=2/11` because most current cards publish only `Through ...` or `Ongoing` date text on the official listing page.
- Confirmed `npm run compare:staging -- --baseline data/staging/mcny-exhibitions.json --candidate data/staging/mcny-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:mcny:live -- --verified-at 2026-06-25T16:56:00.000Z --apply`, which marked both MCNY source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Added `scripts/exhibit-ingest/sources/planned/fit-exhibitions.md` to nominate Museum at FIT as the next official NYC source after MCNY, based on the 2026-06-25 live fetch check and the visible server-rendered `Current` plus `Upcoming` long-card sections that are present on the main exhibitions route.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, `EXHIBITION_BACKEND_BRIEF.md`, and `scripts/exhibit-ingest/sources/planned/mcny-exhibitions.md` so the repo now reflects MCNY as another completed verified-live staging source and moves the next-source queue forward to FIT.
- Verified `node --test scripts/exhibit-ingest/parsers/mcny-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:mcny:stage`, `npm run review:staging:mcny:summary`, `npm run review:staging:mcny:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/mcny-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/mcny-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/mcny-exhibitions.json --candidate data/staging/mcny-exhibitions.live.json`, `npm run verify:source:mcny:live -- --verified-at 2026-06-25T16:56:00.000Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining next-source work: capture a checked-in FIT exhibitions fixture, add fixture/live source configs, and build the first parser slice for the visible `Current` plus `Upcoming` long cards while deciding whether non-gallery cards like `Galleries Closed` belong in the first reviewer inbox.

## 2026-06-25 Morgan Completion And MCNY Discovery Slice

- Added `scripts/exhibit-ingest/parsers/morgan-exhibitions.mjs` and `scripts/exhibit-ingest/parsers/morgan-exhibitions.test.mjs`, so The Morgan Library & Museum now has a dedicated exhibition parser for the official `current` and `upcoming` routes that keeps only the main listing grids and excludes the separate current-page `Collection Spotlight` block.
- Added fixture/live Morgan source configs plus source-specific staging, review, and live-verification package scripts, registered the parser in `scripts/exhibit-ingest/ingest.mjs`, and extended source-config validation coverage so Morgan plugs into the same staging-first backend contract as the existing official NYC sources.
- Generated `data/staging/morgan-exhibitions.json` and `data/staging/morgan-exhibitions.live.json`; the current Morgan reviewer inbox stages 9 pending creates from 2 configured official pages, is now `verified_live`, and keeps the remaining blocker explicit at `startDate=8/9` because `J. Pierpont Morgan's Library` is still listed only as `Ongoing` on the official source page.
- Confirmed `npm run compare:staging -- --baseline data/staging/morgan-exhibitions.json --candidate data/staging/morgan-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:morgan:live -- --verified-at 2026-06-25T16:46:41.034Z --apply`, which marked both Morgan source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Added `scripts/exhibit-ingest/sources/planned/mcny-exhibitions.md` to nominate Museum of the City of New York as the next official NYC source after Morgan, based on the 2026-06-25 live fetch check and the visible server-rendered `Exhibitions On View` plus `Upcoming Exhibitions` card sections that can be isolated from the mixed `Online Exhibitions` and `Traveling Exhibitions` blocks.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, `EXHIBITION_BACKEND_BRIEF.md`, and `scripts/exhibit-ingest/sources/planned/morgan-exhibitions.md` so the repo now reflects Morgan as another completed verified-live staging source and moves the next-source queue forward to MCNY.
- Verified `node --test scripts/exhibit-ingest/parsers/morgan-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:morgan:stage`, `npm run review:staging:morgan:summary`, `npm run review:staging:morgan:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/morgan-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/morgan-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/morgan-exhibitions.json --candidate data/staging/morgan-exhibitions.live.json`, `npm run verify:source:morgan:live -- --verified-at 2026-06-25T16:46:41.034Z`, and the same command with `--apply`.
- Remaining next-source work: capture a checked-in MCNY exhibitions fixture, add fixture/live source configs, and build the first parser slice for the official `Exhibitions On View` plus `Upcoming Exhibitions` cards while excluding `Online Exhibitions` and `Traveling Exhibitions`.

## 2026-06-25 Noguchi Source Completion Slice

- Added `scripts/exhibit-ingest/parsers/noguchi-exhibitions.mjs` and `scripts/exhibit-ingest/parsers/noguchi-exhibitions.test.mjs`, so The Noguchi Museum now has a dedicated exhibition parser that keeps only the museum-side listing cards before the first explicit `offsite:` entry and dedupes duplicated desktop/mobile blocks by exhibition URL.
- Added fixture/live Noguchi source configs plus source-specific staging, review, and live-verification package scripts, then registered the parser in `scripts/exhibit-ingest/ingest.mjs` and extended source-config validation coverage so Noguchi plugs into the same staging-first backend contract as the existing official NYC sources.
- Generated `data/staging/noguchi-exhibitions.json` and `data/staging/noguchi-exhibitions.live.json`; the current Noguchi reviewer inbox stages 2 pending creates, is `ready_for_human_review` on the minimum field contract, and keeps the remaining optional gap explicit at `description=0/2`.
- Confirmed `npm run compare:staging -- --baseline data/staging/noguchi-exhibitions.json --candidate data/staging/noguchi-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:noguchi:live -- --verified-at 2026-06-25T14:52:00.000Z --apply`, which marked both Noguchi source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, `EXHIBITION_BACKEND_BRIEF.md`, and `scripts/exhibit-ingest/sources/planned/morgan-exhibitions.md` so the repo now reflects Noguchi as another completed verified-live staging source and moves the next-source queue forward to Morgan.
- Verified `node --test scripts/exhibit-ingest/parsers/noguchi-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:noguchi:stage`, `npm run review:staging:noguchi:summary`, `npm run review:staging:noguchi:json`, `node --test scripts/exhibit-ingest/source-config-staging-alignment.test.mjs scripts/exhibit-ingest/staging-artifacts.test.mjs`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/noguchi-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/noguchi-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/noguchi-exhibitions.json --candidate data/staging/noguchi-exhibitions.live.json`, `npm run verify:source:noguchi:live -- --verified-at 2026-06-25T14:52:00.000Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining next-source work: add Morgan fixture/live source configs, build the first current/upcoming listing parser, and decide whether the Morgan `Collection Spotlight` block belongs in the same source or stays excluded from the first reviewer inbox.

## 2026-06-25 Frick Completion And Noguchi Discovery Slice

- Finished the first Frick backend slice end to end instead of leaving it in discovery-only status: the checked-in Frick parser/tests, fixture/live source configs, source-specific staging/review/live-verification scripts, and staged reviewer artifacts now align and verify cleanly.
- Re-ran the fixture-backed Frick staging and reviewer flows plus a fresh live staging artifact to `data/staging/frick-exhibitions.live.json`; the Frick source stages 4 pending `create` items, remains `verified_live`, and is already `ready_for_human_review` with full minimum-field coverage from the official listing page alone.
- Confirmed `npm run compare:staging -- --baseline data/staging/frick-exhibitions.json --candidate data/staging/frick-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:frick:live -- --verified-at 2026-06-25T14:31:17.724Z --apply`, which refreshed both Frick source configs plus the fixture-backed review artifact metadata without touching canonical data.
- Added `scripts/exhibit-ingest/sources/planned/noguchi-exhibitions.md` to nominate The Noguchi Museum as the next official NYC source after Frick, based on the 2026-06-25 live fetch check and the explicit on-site versus `offsite:` split visible in the official page HTML.
- Captured `scripts/exhibit-ingest/fixtures/noguchi-exhibitions.html` from `https://www.noguchi.org/museum/exhibitions/current/` and documented the first safe parser boundary: dedupe the desktop/mobile duplicate cards and stop before the first explicit `offsite:` listing so non-NYC records do not enter staging.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects Frick as another verified-live staging source and carries the queue forward to Noguchi as the next focused discovery target.
- Verified `node --test scripts/exhibit-ingest/parsers/frick-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/source-config-staging-alignment.test.mjs`, `npm run ingest:exhibits:frick:stage`, `npm run review:staging:frick:summary`, `npm run review:staging:frick:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/frick-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/frick-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/frick-exhibitions.json --candidate data/staging/frick-exhibitions.live.json`, `npm run verify:source:frick:live -- --verified-at 2026-06-25T14:31:17.724Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining next-source work: add Noguchi fixture/live source configs and a staging-only parser that keeps only on-site museum exhibitions while excluding offsite cards and detail-page enrichment.

## 2026-06-25 MAD Detail Enrichment Slice

- Expanded the verified-live MAD source from a listing-only slice into an index-plus-detail staging flow by enabling `followRecordUrls` in both MAD source configs, adding checked-in official detail fixtures under `scripts/exhibit-ingest/fixtures/mad-exhibitions-details/`, and teaching `scripts/exhibit-ingest/parsers/mad-exhibitions.mjs` to parse official detail pages when the ingest pipeline follows exhibition URLs.
- The MAD parser now keeps the existing `Current Exhibitions` plus `Upcoming` listing scope, but merges official detail-page `og:title`, `og:image`, visible exhibition date headings, and the main body copy into the staged exhibition records without touching canonical data or reviewer statuses.
- Regenerated `data/staging/mad-exhibitions.json` and `data/staging/mad-exhibitions.live.json`; the current MAD reviewer inbox still stages 6 pending creates, now shows `configured=1 followed=6`, improves optional reviewer metadata coverage from `description=0/6` to `description=6/6`, and extends the single upcoming exhibition to a full `startDate` plus `endDate` range.
- Confirmed the updated fixture-backed and live MAD artifacts still match with `added=0 removed=0 changed=0`, then applied `npm run verify:source:mad:live -- --verified-at 2026-06-25T14:30:30.000Z --apply`, which refreshed both MAD source configs plus the fixture-backed staging report to the new verified-live timestamp for the index-plus-detail slice.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects MAD as a verified-live index-plus-detail source and keeps the remaining product/backend decision narrow: whether another official MAD path can surface exact start dates for the 5 current exhibitions or whether end-date-only current records should remain below the minimum review contract.
- Verified `node --test scripts/exhibit-ingest/parsers/mad-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:mad:stage`, `npm run review:staging:mad:summary`, `npm run review:staging:mad:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/mad-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/mad-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/mad-exhibitions.json --candidate data/staging/mad-exhibitions.live.json`, `npm run verify:source:mad:live -- --verified-at 2026-06-25T14:30:30.000Z`, `npm run verify:source:mad:live -- --verified-at 2026-06-25T14:30:30.000Z --apply`, and `npm run build`.
- `npm test` still fails on pre-existing Frick work-in-progress gaps outside this MAD slice: one parser expectation spacing mismatch in `scripts/exhibit-ingest/parsers/frick-exhibitions.test.mjs`, and one missing checked-in artifact expected by `scripts/exhibit-ingest/source-config-staging-alignment.test.mjs` at `data/staging/frick-exhibitions.json`.
- Remaining MAD work: determine whether the official MAD site exposes recoverable exact start dates for the 5 current exhibitions anywhere beyond the followed detail pages; if not, decide whether the minimum review contract should explicitly allow trusted current exhibitions with `endDate` but no exact `startDate`.

## 2026-06-25 Frick Discovery And Fixture Slice

- Confirmed from the current backend docs and queue that David Zwirner remains complete for the present staging-and-review milestone, so this run did not reopen the legacy prototype and instead advanced the next-source backlog.
- Checked several official NYC exhibition pages live on 2026-06-25 and kept The Frick Collection as the safest next source candidate from this environment: `https://www.frick.org/exhibitions` returned fetchable HTML with visible `current` and `upcoming` sections, while MoMA returned a Cloudflare challenge, Brooklyn Museum returned a Vercel security checkpoint, and the Met continued to return a challenge response.
- Added `scripts/exhibit-ingest/sources/planned/frick-exhibitions.md` to document the official Frick exhibitions page, the observed reviewer-useful fields, why MCNY and Morgan are weaker first parser targets, the explicit first-slice exclusions (`Past`, `Virtual Exhibitions`, detail-page enrichment), and the smallest safe staging-only parser target.
- Captured `scripts/exhibit-ingest/fixtures/frick-exhibitions.html` from `https://www.frick.org/exhibitions` and verified the checked-in fixture contains the stable `#current` and `#upcoming` anchors plus visible card-level title, date text, description, image, and `Read More` link structure needed for the first parser slice.
- Updated `BACKEND_TASKS.md` and `EXHIBITION_BACKEND_BRIEF.md` so the queue now records the completed Frick discovery-and-fixture slice while keeping MAD itself staging-only and unchanged until any detail-page enrichment decision is made.
- Verified live fetch checks for The Frick Collection, MoMA, Brooklyn Museum, the Met, Morgan, and MCNY; then verified the checked-in Frick fixture markers with `rg`, followed by `npm test` and `npm run build`.
- Remaining Frick work: add fixture/live source configs and implement a conservative current/upcoming parser before generating any staged reviewer inbox.

## 2026-06-24 MAD Source Completion Slice

- Added `scripts/exhibit-ingest/parsers/mad-exhibitions.mjs`, `scripts/exhibit-ingest/parsers/mad-exhibitions.test.mjs`, fixture/live MAD source configs, source-specific staging/review/live-verification package scripts, and a checked-in fixture at `scripts/exhibit-ingest/fixtures/mad-exhibitions.html` so the Museum of Arts and Design now plugs into the same staging-first backend contract as the other official NYC sources.
- The MAD parser stages only the official `Current Exhibitions` and `Upcoming` listing grids from `https://madmuseum.org/exhibitions`, resolves relative image/detail URLs, preserves reviewer-facing `dateText`, and intentionally excludes `Installations`, `Past`, and detail-page enrichment from this first slice.
- Generated `data/staging/mad-exhibitions.json` and `data/staging/mad-exhibitions.live.json`; the current MAD reviewer inbox stages 6 pending creates from 1 configured index page, is now `verified_live`, and makes the remaining minimum-review blocker explicit at `startDate=1/6` with `description=0/6`.
- Confirmed `npm run compare:staging -- --baseline data/staging/mad-exhibitions.json --candidate data/staging/mad-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:mad:live -- --verified-at 2026-06-24T22:10:03.615Z --apply`, which marked both MAD source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects MAD as another live-verified staging source and keeps the next MAD decision focused on whether detail-page enrichment is worth adding for missing `startDate` and `description` coverage.
- Verified `node --test scripts/exhibit-ingest/parsers/mad-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:mad:stage`, `npm run review:staging:mad:summary`, `npm run review:staging:mad:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/mad-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/mad-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/mad-exhibitions.json --candidate data/staging/mad-exhibitions.live.json`, and `npm run verify:source:mad:live -- --verified-at 2026-06-24T22:10:03.615Z` plus the same command with `--apply`.
- Remaining MAD work: decide whether to follow official detail pages for exact `startDate` values and optional `description` enrichment before treating the source as minimum-ready for human review.

## 2026-06-24 Drawing Center Source Completion Slice

- Added `scripts/exhibit-ingest/parsers/drawing-center-exhibitions.mjs`, fixture/live Drawing Center source configs, source-specific staging/review/live-verification package scripts, and a checked-in fixture at `scripts/exhibit-ingest/fixtures/drawing-center-exhibitions.html` so The Drawing Center now plugs into the same staging-first backend contract as the other official NYC sources.
- The Drawing Center parser stages only the official `onview` and `upcoming` listing modules from `https://drawingcenter.org/exhibitions`, keeps `past` and detail-page enrichment out of scope, and uses the same official index-page Nuxt bootstrap only as a narrow fallback for fields the visible listing omits, which brings the staged reviewer inbox up to `startDate=3/3` while keeping the source reviewable.
- Added `scripts/exhibit-ingest/parsers/drawing-center-exhibitions.test.mjs` plus `scripts/exhibit-ingest/source-config-staging-alignment.test.mjs`, and extended `scripts/exhibit-ingest/schema-validation.test.mjs`, so checked-in fixture source configs now stay aligned with their staged reviewer inbox metadata and the new Drawing Center source remains covered in the backend suite.
- Generated `data/staging/drawing-center-exhibitions.json` and refreshed `data/staging/drawing-center-exhibitions.live.json`; the current Drawing Center reviewer inbox stages 3 pending creates, is `verified_live`, is `ready_for_human_review`, and still keeps the remaining optional gaps explicit at `description=0/3` and `imageUrl=2/3`.
- Confirmed `npm run compare:staging -- --baseline data/staging/drawing-center-exhibitions.json --candidate data/staging/drawing-center-exhibitions.live.json` reports `added=0 removed=0 changed=0`, and re-ran `npm run verify:source:drawing-center:live -- --verified-at 2026-06-24T19:23:50.634Z` as an eligible dry run after the parser fallback change without promoting anything to canonical data.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects The Drawing Center as another verified-live staging source and keeps Museum of Arts and Design as the next focused official NYC source candidate.
- Verified `node --test scripts/exhibit-ingest/parsers/drawing-center-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/source-config-staging-alignment.test.mjs`, `npm run ingest:exhibits:drawing-center:stage`, `npm run review:staging:drawing-center:summary`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/drawing-center-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/drawing-center-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/drawing-center-exhibitions.json --candidate data/staging/drawing-center-exhibitions.live.json`, `npm run verify:source:drawing-center:live -- --verified-at 2026-06-24T19:23:50.634Z`, `npm test`, and `npm run build`.
- Remaining next-source work: capture the Museum of Arts and Design index fixture, add fixture/live source configs, and build a current/upcoming-only staging parser before deciding whether `Installations` belongs in its own source.

## 2026-06-24 Guggenheim Source Slice And MAD Discovery Handoff

- Added the first Guggenheim exhibition source end to end in staging-only mode: `scripts/exhibit-ingest/parsers/guggenheim-exhibitions.mjs` now reads the official `https://www.guggenheim.org/exhibitions` bootstrap payload and stages only the embedded `on_view` plus `upcoming` exhibition items.
- Captured the official Guggenheim exhibitions fixture at `scripts/exhibit-ingest/fixtures/guggenheim-exhibitions.html`, added fixture/live source configs, registered source-specific staging/review/live-verification package scripts, and extended schema-validation coverage so Guggenheim plugs into the same backend contract as the existing official sources.
- Generated `data/staging/guggenheim-exhibitions.json` and `data/staging/guggenheim-exhibitions.live.json`; the current Guggenheim reviewer inbox stages 6 pending creates, covers `description` and `imageUrl` on all 6 records from the official bootstrap payload, and keeps the source safely staging-only.
- Confirmed `npm run compare:staging -- --baseline data/staging/guggenheim-exhibitions.json --candidate data/staging/guggenheim-exhibitions.live.json` reports `added=0 removed=0 changed=0`, then applied `npm run verify:source:guggenheim:live -- --verified-at 2026-06-24T19:18:00.000Z --apply`, which marked both Guggenheim source configs `verified_live` and refreshed the fixture-backed reviewer artifact metadata without touching canonical records.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` so the repo now reflects Guggenheim as a live-verified review-ready source and records Museum of Arts and Design as the next focused official NYC source candidate.
- Added `scripts/exhibit-ingest/sources/planned/mad-exhibitions.md` after checking current official-source fetchability on 2026-06-24: `https://madmuseum.org/exhibitions` returned `HTTP 200` and exposes clear server-rendered `Current Exhibitions` and `Upcoming` grids, making it a stronger next parser target than sources that still gate access or blur installations into the main list.
- Verified `node --test scripts/exhibit-ingest/parsers/guggenheim-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:guggenheim:stage`, `npm run review:staging:guggenheim:summary`, `npm run review:staging:guggenheim:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/guggenheim-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/guggenheim-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/guggenheim-exhibitions.json --candidate data/staging/guggenheim-exhibitions.live.json`, `npm run verify:source:guggenheim:live -- --verified-at 2026-06-24T19:18:00.000Z`, `npm run verify:source:guggenheim:live -- --verified-at 2026-06-24T19:18:00.000Z --apply`, `npm test`, and `npm run build`.
- Remaining next-source work: decide whether Guggenheim needs reviewer-visible warnings for stale-looking on-view dates or detail-page enrichment, and start the MAD source with a fixture capture plus a current/upcoming-only staging parser.

## 2026-06-24 Jewish Museum Backend Catch-Up Slice

- Brought the checked-in backend code up to the already-documented Jewish Museum source state by adding `scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.mjs`, its focused parser tests, and the fixture/live Jewish Museum source configs used by the staging pipeline.
- Registered the Jewish Museum parser in `scripts/exhibit-ingest/ingest.mjs`, extended source-config schema coverage, and added the missing `verify:source:jewish-museum:live` package script so the source now has the same source-specific staging, review, compare, and verification command surface as the other official venue adapters.
- Re-ran the Jewish Museum fixture-backed staging and reviewer flows plus a fresh live staging artifact to `data/staging/jewish-museum-exhibitions.live.json`; the source still stages 8 pending `create` items, remains `ready_for_human_review`, and still matches live with `added=0 removed=0 changed=0`.
- Kept the broader queue unchanged after the catch-up: The Jewish Museum stays staging-only with `description=0/8` as its remaining optional metadata gap, while The Drawing Center remains the next focused source-expansion target and Guggenheim remains the documented alternate candidate.
- Verified `node --test scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:jewish-museum:stage`, `npm run review:staging:jewish-museum:summary`, `npm run review:staging:jewish-museum:json`, `npm run ingest:exhibits:jewish-museum:live:stage -- --output data/staging/jewish-museum-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/jewish-museum-exhibitions.json --candidate data/staging/jewish-museum-exhibitions.live.json`, `npm run verify:source:jewish-museum:live -- --verified-at 2026-06-24T17:25:23.527Z`, `npm test`, and `npm run build`.

## 2026-06-24 Drawing Center Next-Source Decision

- Kept the Jewish Museum source staging-only and `verified_live`, then used fresh live fetch checks to reassess the safest next official NYC source candidate from this environment.
- Added `scripts/exhibit-ingest/sources/planned/drawing-center-exhibitions.md` to document The Drawing Center as the preferred next-source discovery target because `https://drawingcenter.org/exhibitions` returned `200` on 2026-06-24 and exposes server-rendered `onview` plus `upcoming` listing markup with reviewer-useful title, date, link, and image fields.
- Preserved the existing Guggenheim discovery note as an alternate candidate, but updated the backend queue to prefer The Drawing Center because Guggenheim's data is embedded in a larger bootstrap payload and is a less conservative first parser target than The Drawing Center's server-rendered listing HTML.
- Updated `BACKEND_TASKS.md` and `EXHIBITION_BACKEND_BRIEF.md` so the active post-Jewish Museum queue now points to The Drawing Center fixture capture and a listing-only parser before any detail-page enrichment.
- Verified the candidate choice with live fetch checks against `https://drawingcenter.org/exhibitions`, `https://www.guggenheim.org/exhibitions`, `https://www.moma.org/calendar/exhibitions`, `https://www.brooklynmuseum.org/exhibitions`, and `https://www.metmuseum.org/exhibitions/listings`.
- Remaining next-source work: capture the Drawing Center index fixture and build the first staging-only parser for the server-rendered `onview` and `upcoming` sections.

## 2026-06-24 Jewish Museum Verification Pass

- Re-ran the Jewish Museum fixture-backed staging and reviewer flows sequentially so the checked-in reviewer inbox reflects one consistent artifact timestamp and remains safely staging-only.
- Confirmed `npm run compare:staging -- --baseline data/staging/jewish-museum-exhibitions.json --candidate data/staging/jewish-museum-exhibitions.live.json` still reports `added=0 removed=0 changed=0`, so the fixture-backed and live Jewish Museum artifacts remain in parity.
- Confirmed `npm run verify:source:jewish-museum:live -- --verified-at 2026-06-24T17:25:23.527Z` is still eligible with zero blockers or warnings, preserving the current `verified_live` source metadata without applying any canonical changes.
- Reconfirmed the current Jewish Museum review state: 8 pending `create` items, `ready_for_human_review` readiness on minimum required fields, `current=5` and `upcoming=3`, and the remaining optional-metadata gap of `description=0/8`.
- Verified `npm run ingest:exhibits:jewish-museum:stage`, `npm run review:staging:jewish-museum:summary`, `npm run review:staging:jewish-museum:json`, `npm run compare:staging -- --baseline data/staging/jewish-museum-exhibitions.json --candidate data/staging/jewish-museum-exhibitions.live.json`, `npm run verify:source:jewish-museum:live -- --verified-at 2026-06-24T17:25:23.527Z`, `npm test`, and `npm run build`.
- Remaining next-source work: choose whether the next smallest safe slice is Jewish Museum detail-page enrichment for missing descriptions or Drawing Center fixture capture plus its first listing-only parser.

## 2026-06-24 ICP Description Boundary Cleanup Slice

- Tightened `scripts/exhibit-ingest/parsers/icp-exhibitions.mjs` so ICP detail-page description extraction now prefers the official exhibition-text body block over unrelated cookie, embed, or newsletter blocks, keeps paragraph breaks, and stops before supplementary sections such as `About ...`, `Featured Photobooks`, and `Header image`.
- Expanded `scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs` with focused coverage for the cleaned description boundaries, including the promotional-tail and appendix cutoffs.
- Regenerated `data/staging/icp-exhibitions.json`; the ICP reviewer inbox remains `verified_live` and `ready_for_human_review`, stages 3 current exhibition creates from 1 configured index page plus 3 followed detail pages, and now keeps staged descriptions focused on exhibition summaries instead of biography, featured-list, or newsletter text.
- Aligned `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` with the current repo state, including the already-existing verified-live Jewish Museum staging slice and the next-source handoff to Guggenheim discovery.
- Re-ran the ICP fixture-backed and live staging flow sequentially so the source-specific reviewer outputs reference the same artifact and the checked-in fixture/live parity remains explicit after the parser cleanup.
- Verified `node --test scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:icp:stage`, `npm run review:staging:icp:summary`, `npm run review:staging:icp:json`, `node scripts/exhibit-ingest/ingest.mjs --source scripts/exhibit-ingest/sources/icp-exhibitions.json --records data/exhibit-records.json --stage --output data/staging/icp-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/icp-exhibitions.json --candidate data/staging/icp-exhibitions.live.json`, `npm run verify:source:icp:live -- --verified-at 2026-06-24T18:40:00.000Z`, `npm test`, and `npm run build`.
- Remaining next-source work: keep the Jewish Museum flow staging-only and decide whether detail-page enrichment or the client-side schedule calendar should be the next separate slice before moving beyond the current Guggenheim discovery notes.

## 2026-06-24 Jewish Museum Parser Completion And Guggenheim Discovery Slice

- Repaired `scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.mjs` so it now parses each visible Jewish Museum card block in two steps instead of relying on one brittle regex; this matches the real server-rendered card order where the primary exhibition image appears before the category, title, and date text.
- Tightened the Jewish Museum parser cutoff logic so the first staging slice stops before either the `#schedule` wrapper or the `#react-exhibitions-calendar` mount, keeping the current source limited to visible server-rendered exhibition cards.
- Regenerated `data/staging/jewish-museum-exhibitions.json`; the Jewish Museum source now stages 8 pending creates with `verified_live` readiness, full minimum required field coverage, and the expected remaining optional gap of `description=0/8`.
- Verified `node --test scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.test.mjs`, `npm run ingest:exhibits:jewish-museum:stage`, `npm run review:staging:jewish-museum:summary`, `npm run review:staging:jewish-museum:json`, `npm test`, and `npm run build`.
- Added `scripts/exhibit-ingest/sources/planned/guggenheim-exhibitions.md` to nominate Guggenheim as the next official NYC source after The Jewish Museum, based on the currently fetchable official exhibitions page and its embedded structured `on_view` and `upcoming` bootstrap data.
- Updated `BACKEND_TASKS.md` so The Jewish Museum is treated as the current verified-live staging source with a separate follow-up decision on optional enrichment, and the next discovery target now moves to Guggenheim.

## 2026-06-24 ICP Description Cleanup And Jewish Museum Repair Slice

- Tightened `scripts/exhibit-ingest/parsers/icp-exhibitions.mjs` so ICP detail-page description extraction now prefers the official exhibition-text block, trims inline promotional tails such as `To see more from ...`, and stops before `About ...`, `Featured Photobooks`, and `Header image` sections instead of staging reviewer-noisy biography, newsletter, or featured-list content.
- Expanded `scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs` with coverage for the new description-boundary rules, including promotional-link and featured-list cutoffs.
- Regenerated `data/staging/icp-exhibitions.json`; the ICP reviewer inbox remains `verified_live` and `ready_for_human_review`, but the staged descriptions now stay focused on exhibition summaries.
- Repaired `scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.mjs` so the parser matches the real server-rendered Jewish Museum card order, where `img[data-src]` appears before the category and date text.
- Verified `node --test scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:icp:stage`, `npm run review:staging:icp:summary`, `npm run review:staging:icp:json`, `node --test scripts/exhibit-ingest/parsers/jewish-museum-exhibitions.test.mjs`, `npm test`, and `npm run build`.
- Remaining next-source work: wire the already-started Jewish Museum source into the end-to-end staging workflow with ingest parser registration, package scripts, review artifact generation, and source-specific verification commands before any detail-page or React-calendar expansion.

## 2026-06-24 ICP Fixture-Parity Validation Slice

- Tightened `schemas/exhibition-source.schema.json` so any fixture-backed source page that sets `followRecordUrls: true` must also declare `followRecordUrlFixtureDirectory`; this prevents offline staging/review runs from silently falling back to live detail-page fetches.
- Added `pageRole` to staged `summary.sourcePages` entries so every fetched source page is now labeled as either a configured listing page or a followed detail page, then surfaced configured/followed plus fixture/live counts in `scripts/exhibit-ingest/review-staging.mjs` for reviewer audit.
- Added and refreshed coverage in `scripts/exhibit-ingest/ingest.test.mjs`, `scripts/exhibit-ingest/review-staging.test.mjs`, `scripts/exhibit-ingest/compare-staging.test.mjs`, `scripts/exhibit-ingest/verify-source-live.test.mjs`, and `scripts/exhibit-ingest/schema-validation.test.mjs` so the backend suite reflects the `pageRole`-aware staging schema and multi-page review output.
- Updated `BACKEND_OWNER_MANUAL.md`, `BACKEND_TASKS.md`, and `EXHIBITION_BACKEND_BRIEF.md` to match the actual ICP backend state: detail-page enrichment is already active, reviewer artifacts are `verified_live`, configured-vs-followed page coverage is visible in review output, and fixture parity is part of the source contract.
- Re-ran the ICP fixture-backed staging and review flow sequentially to avoid the shared-file race noted in the owner manual. The current checked-in ICP reviewer inbox now stages 3 pending creates, fetches 1 configured index page plus 3 followed detail fixtures, and shows full `description`/`imageUrl` coverage with `ready_for_human_review` readiness.
- Verified `node --test scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/review-staging.test.mjs scripts/exhibit-ingest/compare-staging.test.mjs scripts/exhibit-ingest/verify-source-live.test.mjs`, `npm test`, `npm run build`, `npm run ingest:exhibits:icp:stage`, `npm run review:staging:icp:summary`, and `npm run review:staging:icp:json`.
- Remaining ICP decision: keep the current verified-live current/upcoming slice staging-only, then choose whether the next safe move is a new institution or a separately modeled ICP section such as `future-exhibitions`.

## 2026-06-24 Jewish Museum Next-Source Discovery Slice

- Confirmed that David Zwirner remains complete for the current staging/review milestone, so this run stayed off the legacy prototype and moved the queue forward to the next official NYC source after ICP.
- Added `scripts/exhibit-ingest/sources/planned/jewish-museum-exhibitions.md` to record The Jewish Museum exhibitions page as the next source candidate, including the official URL, the observed server-rendered card structure, and the smallest safe staging-only slice.
- Documented why The Jewish Museum is the best current candidate in this environment: `https://thejewishmuseum.org/exhibitions/` returns normal HTML with visible exhibition card data, while `https://www.metmuseum.org/exhibitions` and `https://www.brooklynmuseum.org/exhibitions` currently return `HTTP 429` challenge responses here.
- Updated `BACKEND_TASKS.md` so the post-ICP queue now points to fixture capture and a conservative first parser for The Jewish Museum instead of leaving the next source undefined.
- Verified `curl -I -L` fetchability checks for The Jewish Museum, Guggenheim, the Met, and Brooklyn Museum plus direct HTML structure inspection for The Jewish Museum and Guggenheim, and kept the backend queue change documentation-only for this slice.
- Remaining next-source work: capture the Jewish Museum index fixture, add fixture/live source configs, and build the first staging-only parser for the server-rendered exhibition cards before deciding whether detail pages or the React calendar belong in the same review workflow.

## 2026-06-24 ICP Initial Source Slice

- Added a fixture snapshot of the official ICP exhibitions page at `scripts/exhibit-ingest/fixtures/icp-exhibitions.html` and documented ICP as the next focused official NYC source in `scripts/exhibit-ingest/sources/planned/icp-exhibitions.md`.
- Added `scripts/exhibit-ingest/parsers/icp-exhibitions.mjs`, which stages ICP `current` and `upcoming` exhibition cards from the main exhibitions index page, extracts exact `startDate` and `endDate` values from the published card date ranges, and explicitly excludes `past` exhibitions plus the separate `future-exhibitions` landing page from this first slice.
- Added ICP fixture/live source configs, source-specific staging/review/live-verification package scripts, and parser/schema coverage so ICP now plugs into the same review-first staging workflow as New Museum, David Zwirner, and Whitney.
- Generated `data/staging/icp-exhibitions.json`, then ran a live ICP staging artifact to `data/staging/icp-exhibitions.live.json`; the fixture and live runs matched with `added=0 removed=0 changed=0`, so both ICP source configs are now marked `verified_live` at `2026-06-24T02:20:00.000Z`.
- Verified `node --test scripts/exhibit-ingest/parsers/icp-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs scripts/exhibit-ingest/ingest.test.mjs`, `npm run ingest:exhibits:icp:stage`, `npm run review:staging:icp:summary`, `npm run review:staging:icp:json`, the live ICP staging command to `data/staging/icp-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/icp-exhibitions.json --candidate data/staging/icp-exhibitions.live.json`, `npm run verify:source:icp:live -- --verified-at 2026-06-24T02:20:00.000Z`, the same command with `--apply`, `npm test`, and `npm run build`.
- Remaining ICP gap: the source is now `ready_for_human_review` on minimum required fields, but `description` is missing on all 4 staged exhibitions, so any detail-page enrichment should remain a separate staging-only follow-up.

## 2026-06-23 ICP Next-Source Discovery Slice

- Confirmed that David Zwirner is complete for the current staging/review milestone and that Whitney now occupies the active post-Zwirner ingestion slot, so this run shifted to documenting the next source after Whitney instead of widening the existing parsers.
- Added `scripts/exhibit-ingest/sources/planned/icp-exhibitions.md` to record ICP exhibitions as the next official NYC source candidate after Whitney, including the official URL, the currently observed HTML structure, and the smallest safe staging-only slice.
- Documented why ICP is the safer next target in this environment right now: `https://www.icp.org/exhibitions` is fetchable without a checkpoint and already exposes current/upcoming exhibition cards with title, date text, detail URL, and image, while MoMA and Brooklyn Museum currently return bot/security challenges here.
- Updated `BACKEND_TASKS.md` so the post-Whitney queue now points to ICP fixture capture, source configs, and a current/upcoming-only first staging slice rather than leaving the next source undefined.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:whitney:stage`, and `npm run review:staging:whitney:summary`.
- Remaining next-source work: capture the ICP index fixture, add ICP fixture/live source configs, and build the first staging-only parser for the current/upcoming exhibition cards before deciding whether other ICP exhibition sub-sections belong in the same review workflow.

## 2026-06-24 Whitney Review Scope Visibility Slice

- Updated `scripts/exhibit-ingest/ingest.mjs` and `schemas/exhibition-staging.schema.json` so staging reports now preserve source-config `stagingNotes` in `summary`, keeping source-scope decisions in the artifact itself instead of only in source JSON files.
- Updated `scripts/exhibit-ingest/review-staging.mjs` so reviewer text output now prints a top-level `Scope:` line and reviewer JSON payloads include `source.stagingNotes`, which makes Whitney's intentional `current`/`upcoming` in-person slice and `online`/`public art` exclusions explicit during review.
- Added coverage in `scripts/exhibit-ingest/ingest.test.mjs`, `scripts/exhibit-ingest/review-staging.test.mjs`, and schema validation tests for the new scope metadata.
- Regenerated `data/staging/whitney-exhibitions.json`; the Whitney source still stages 7 pending creates with `verified_live` readiness, and the review artifact now shows the exact source slice at the top of the reviewer summary.
- Verified `node --test scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/review-staging.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:whitney:stage`, and a direct Whitney reviewer summary run against `data/staging/whitney-exhibitions.json`.
- Remaining Whitney decision: keep the current in-person `current`/`upcoming` slice staging-only and decide separately whether `online` and `public art` need their own review model or should remain excluded.

## 2026-06-23 Whitney Source Notes Alignment Slice

- Updated `scripts/exhibit-ingest/parsers/whitney-exhibitions.mjs` so Whitney list-page records no longer tell reviewers that detail-page parsing is still pending after the index-plus-detail enrichment flow runs.
- Whitney staged `sourceNotes` now explain the current behavior accurately: `dateText` is preserved as reviewer-facing list-page context, while linked official detail pages supply exact dates and richer optional metadata when available.
- Regenerated `data/staging/whitney-exhibitions.json`; the source still stages 7 pending creates with `verified_live` readiness, but the merged staged notes now reflect the actual implemented flow instead of stale pre-enrichment wording.
- Verified `node --test scripts/exhibit-ingest/parsers/whitney-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs`, `npm run ingest:exhibits:whitney:stage`, `npm run review:staging:whitney:summary`, `npm run review:staging:whitney:json`, `npm test`, and `npm run build`.
- Remaining Whitney decision: keep the current in-person `current`/`upcoming` slice staging-only and decide separately whether `online` and `public art` need their own review model or should remain excluded.

## 2026-06-23 Whitney Review Gap Detail Slice

- Updated `scripts/exhibit-ingest/review-staging.mjs` so reviewer summaries and JSON payloads now include item-level recommended-gap details for non-blocking field coverage misses, in addition to the existing aggregate coverage counts.
- Added focused review CLI coverage for the new `recommendedGapDetails` payload and text summary output, keeping reviewer-facing gap details stable across staging-only backend changes.
- Re-ran the Whitney fixture-backed staging and review flow; the source still stages 7 pending creates with `verified_live` readiness, and the remaining optional-field gap is now explicit as `Roy Lichtenstein` missing `description` from `https://whitney.org/exhibitions/lichtenstein`.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:whitney:stage`, `npm run review:staging:whitney:summary`, `node scripts/exhibit-ingest/review-staging.mjs --staging data/staging/whitney-exhibitions.json --json`, and `node --test scripts/exhibit-ingest/review-staging.test.mjs scripts/exhibit-ingest/parsers/whitney-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`.
- Remaining Whitney decision: keep the current in-person `current`/`upcoming` slice staging-only and decide separately whether `online` and `public art` need their own review model or should remain excluded.

## 2026-06-23 Whitney Detail Enrichment Slice

- Extended `scripts/exhibit-ingest/ingest.mjs` so source pages can optionally follow discovered record URLs, fetch linked official detail pages, merge repeated record ids conservatively, and expose the full fetched-page audit trail in staging summaries.
- Added Whitney detail-page parsing in `scripts/exhibit-ingest/parsers/whitney-exhibitions.mjs`, using official `ExhibitionEvent` JSON-LD plus metadata to enrich exact `startDate`, `endDate`, and optional `description`/`imageUrl` fields without weakening the staging-only workflow.
- Added checked-in Whitney detail-page fixtures under `scripts/exhibit-ingest/fixtures/whitney-exhibitions-details/` and updated the Whitney fixture/live source configs so the source now stages from the official index plus linked official exhibition detail pages.
- Regenerated `data/staging/whitney-exhibitions.json`; the Whitney review artifact still stages 7 pending creates, but minimum readiness is now satisfied with `startDate=7/7`, `endDate=7/7`, `imageUrl=7/7`, and `description=6/7`.
- Re-ran the live Whitney staging artifact to `data/staging/whitney-exhibitions.live.json` and compared it against the fixture-backed artifact after the new detail-page flow; the runs still matched with `added=0 removed=0 changed=0`, so both Whitney source configs remain `verified_live` at `2026-06-23T18:29:00.000Z`.
- Verified `node --test scripts/exhibit-ingest/parsers/whitney-exhibitions.test.mjs scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:whitney:stage`, `npm run review:staging:whitney:summary`, `npm run review:staging:whitney:json`, the live Whitney staging command to `data/staging/whitney-exhibitions.live.json`, and `npm run compare:staging -- --baseline data/staging/whitney-exhibitions.json --candidate data/staging/whitney-exhibitions.live.json`.
- Remaining Whitney gap: the source is now ready for human review on minimum required fields, but one official detail page still omits `description`, and `online`/`public art` sections remain intentionally out of scope until their review model is defined.

## 2026-06-23 Whitney Initial Source Slice

- Added a fixture snapshot of the official Whitney Museum exhibitions index page at `scripts/exhibit-ingest/fixtures/whitney-exhibitions.html`.
- Added `scripts/exhibit-ingest/parsers/whitney-exhibitions.mjs`, which stages only the Whitney `current` and `upcoming` in-person exhibition cards and intentionally excludes the `online` and `public art` sections from this first review slice.
- Added fixture and live Whitney source configs plus source-specific package scripts for staging and reviewer summary/JSON output.
- Added focused parser coverage for section filtering, HTML entity cleanup, relative image URLs, and partial `Opens ...` date parsing, and extended source-config validation coverage to include the new Whitney configs.
- Generated `data/staging/whitney-exhibitions.json`; the first Whitney review artifact stages 7 pending creates, has full title/venue/source URL/image coverage, and surfaces the current blocker explicitly: `startDate` is populated on only 1 of 7 items because the list page omits exact dates on most cards.
- Ran a live Whitney staging artifact to `data/staging/whitney-exhibitions.live.json` and compared it against the fixture-backed artifact; the live and fixture runs matched with `added=0 removed=0 changed=0`, so both Whitney source configs are now marked `verified_live` at `2026-06-23T14:35:00.000Z`.
- Verified `node --test scripts/exhibit-ingest/parsers/whitney-exhibitions.test.mjs scripts/exhibit-ingest/schema-validation.test.mjs`, `npm run ingest:exhibits:whitney:stage`, `npm run review:staging:whitney:summary`, `npm run review:staging:whitney:json`, the live Whitney staging command to `data/staging/whitney-exhibitions.live.json`, and `npm run compare:staging -- --baseline data/staging/whitney-exhibitions.json --candidate data/staging/whitney-exhibitions.live.json`.
- Remaining Whitney gap: add a detail-page or other official-source date-enrichment step so the source can meet the minimum exhibition-review contract without inventing dates.

## 2026-06-23 David Zwirner Live Verification Completion Slice

- Refreshed the David Zwirner fixture snapshot to match the current official exhibitions index structure, including the current full slug for `In Solidarity` and the embedded `__NEXT_DATA__` image metadata exposed by the live site.
- Updated `scripts/exhibit-ingest/parsers/david-zwirner-exhibitions.mjs` so the parser now reads official embedded `__NEXT_DATA__` exhibition metadata and uses it as a fallback source for richer staged fields such as `imageUrl`, while preserving the existing anchor-based listing flow.
- Updated David Zwirner parser coverage so the fixture-backed test now expects the current official upcoming exhibition slug and current official image URLs.
- Re-ran the fixture-backed staging flow, the live staging flow, the staging comparison, and the live-verification helper; the live David Zwirner artifact now matches the fixture-backed review artifact with `added=0 removed=0 changed=0`.
- Applied live verification metadata to `scripts/exhibit-ingest/sources/david-zwirner-exhibitions.json` and `scripts/exhibit-ingest/sources/david-zwirner-exhibitions.fixture.json` at `2026-06-23T15:00:00.000Z`, then regenerated `data/staging/david-zwirner-exhibitions.json` so reviewer output now reports `verified_live` and `ready_for_human_review`.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, `npm run review:staging:david-zwirner:json`, the live staging command to `data/staging/david-zwirner-exhibitions.live.json`, `npm run compare:staging -- --baseline data/staging/david-zwirner-exhibitions.json --candidate data/staging/david-zwirner-exhibitions.live.json`, `npm run verify:source:david-zwirner:live -- --verified-at 2026-06-23T15:00:00.000Z`, and the same command with `--apply`.
- Selected Whitney Museum exhibitions as the next official NYC source candidate and recorded the initial staging-only discovery notes in `scripts/exhibit-ingest/sources/planned/whitney-exhibitions.md`.

## 2026-06-23 David Zwirner Live Verification Helper Slice

- Added `scripts/exhibit-ingest/verify-source-live.mjs`, a dry-run-first helper that validates live-vs-fixture staging parity and proposes `verified_live` source metadata updates without mutating source configs unless `--apply` is explicitly passed.
- Added focused tests covering eligible live verification, changed-record rejection, and `--apply` updates across both the live and fixture-backed David Zwirner source configs.
- Added `npm run verify:source:live` and `npm run verify:source:david-zwirner:live` so the network-enabled David Zwirner verification handoff is now a documented single-step dry run followed by an explicit apply step.
- Re-ran the David Zwirner fixture-backed staging and review commands sequentially after confirming that parallel staging/review runs can race on the shared staging file and produce stale reviewer output.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner:json`.
- Remaining David Zwirner gap: this environment still cannot fetch the live official exhibitions page, so the actual live staging run, artifact comparison, and `verified_live` metadata apply step remain pending a network-enabled run.

## 2026-06-23 David Zwirner JSON-LD Metadata Slice

- Updated `scripts/exhibit-ingest/parsers/david-zwirner-exhibitions.mjs` so the exhibition parser now merges optional page-level JSON-LD metadata keyed by official exhibition URL, populating staged `description` and `imageUrl` fields when the official source exposes them.
- Extended the David Zwirner fixture with representative official JSON-LD exhibition metadata and added parser assertions that confirm relative and absolute image URLs resolve safely into staging without weakening the review-first workflow.
- Regenerated `data/staging/david-zwirner-exhibitions.json`; the fixture-backed review artifact still stages 6 pending NYC exhibition creates, but optional reviewer metadata coverage improved from `0/6` to `2/6` for both `description` and `imageUrl`.
- Verified `node --test scripts/exhibit-ingest/david-zwirner-exhibitions.test.mjs`, `node --test scripts/exhibit-ingest/ingest.test.mjs scripts/exhibit-ingest/review-staging.test.mjs`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, `npm run review:staging:david-zwirner:json`, `npm test`, and `npm run build`.
- Remaining David Zwirner gap: a network-enabled live staging run still needs to confirm the official exhibitions page exposes compatible listing and JSON-LD metadata before the source config can be marked `verified_live`.

## 2026-06-23 David Zwirner Review Readiness Slice

- Updated `scripts/exhibit-ingest/review-staging.mjs` so reviewer summaries and JSON payloads now expose source readiness status, blockers, warnings, next actions, and minimum exhibition-contract coverage in one staging-only audit surface.
- Review output now reports whether staged items satisfy `title`, `venue`, `startDate`, and `sourceUrl`, and it keeps recommended coverage gaps such as `description` and `imageUrl` visible without mutating staged statuses or canonical data.
- Added focused test coverage for the readiness summary and JSON payload fields, including pending live verification, duplicate/conflict warnings, and minimum-field coverage counts.
- Re-ran the David Zwirner fixture-backed staging flow sequentially with reviewer summary and reviewer JSON so the generated timestamps and review output refer to the same staging artifact.
- Verified `node --test scripts/exhibit-ingest/review-staging.test.mjs`, `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner:json`.
- Remaining David Zwirner gap: the official live exhibitions page still needs a network-enabled staging run plus comparison against the fixture-backed artifact before the source can be marked `verified_live`.

## 2026-06-23 David Zwirner Source Config Validation Slice

- Added `schemas/exhibition-source.schema.json` so backend source configs are now schema-validated before any source page fetch or parser run begins.
- Updated `scripts/exhibit-ingest/schema-validation.mjs` and `scripts/exhibit-ingest/ingest.mjs` so malformed source metadata fails fast instead of producing misleading staging artifacts.
- Added coverage for valid official exhibition configs plus the legacy David Zwirner artist source, and for malformed verification/page metadata that should now be rejected.
- Re-ran the fixture-backed David Zwirner staging flow sequentially with reviewer summary, reviewer JSON, and self-compare verification to confirm the source still stages 6 pending NYC exhibitions and remains `pending_live_verification`.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, `npm run review:staging:david-zwirner:json`, and `npm run compare:staging -- --baseline data/staging/david-zwirner-exhibitions.json --candidate data/staging/david-zwirner-exhibitions.json`.
- Remaining David Zwirner gap: a network-enabled live staging run still needs to be executed and compared before the source config can be marked `verified_live`.

## 2026-06-23 David Zwirner Review Completeness Slice

- Updated `scripts/exhibit-ingest/review-staging.mjs` so reviewer summaries and JSON payloads now expose field-coverage totals for reviewer-relevant exhibition metadata and per-item missing-field notes.
- Added focused test coverage for review completeness output in both text and JSON modes.
- Re-ran the fixture-backed David Zwirner staging flow plus review summary/JSON commands; the source still stages 6 pending NYC exhibition creates, and the current completeness gap is now explicit in review output: `description=0/6` and `imageUrl=0/6`.
- Verified `node --test scripts/exhibit-ingest/review-staging.test.mjs`, `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner:json`.
- Remaining David Zwirner gaps: the official live exhibitions page still needs a network-enabled staging run plus comparison before the source can be marked `verified_live`, and richer official-source fields such as descriptions or images still need a safe staging-only extraction path when available.

## 2026-06-23 David Zwirner Review Insights Slice

- Updated `scripts/exhibit-ingest/review-staging.mjs` so reviewer summaries and JSON payloads now include aggregate venue counts plus `current`/`upcoming` status-tag counts from staged exhibition proposals.
- Added focused test coverage for the new review insights in both text and JSON output, including explicit venue-count ordering and status-tag aggregation.
- Re-ran the David Zwirner fixture-backed staging flow and both review commands sequentially after confirming that parallel staging/review execution can race on the shared staging artifact and produce stale timestamps.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner:json`.
- Remaining David Zwirner gap: the official live exhibitions page still needs a network-enabled staging run plus comparison against the fixture-backed artifact before the source can be marked `verified_live`.

## 2026-06-23 David Zwirner Conflict Staging Slice

- Added initial conflict detection to `scripts/exhibit-ingest/ingest.mjs` so exhibition updates that change reviewer-sensitive fields such as title, venue, dates, or exhibition URLs now stage as `conflict` items instead of ordinary `update` items.
- Conflict items now carry explicit canonical and proposed values plus reviewer-facing notes, which makes official-source disagreements easier to audit without mutating canonical data or reviewer statuses.
- Added focused test coverage for conflict classification in staging reports and for reviewer summary/JSON output that includes conflict sections alongside create, update, and possible-duplicate sections.
- Re-ran the David Zwirner fixture-backed staging and review commands; the current artifact still produces six staged creates and zero conflicts because canonical exhibition data is still empty, which confirms the change stayed staging-only.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner:json`.
- Remaining David Zwirner gap: the official live exhibitions page still needs a network-enabled staging run plus comparison against the fixture-backed artifact before the source can be marked `verified_live`.

## 2026-06-22 David Zwirner Comparison Verification Slice

- Updated `scripts/exhibit-ingest/compare-staging.mjs` so live-vs-fixture staging comparisons now carry `summary.verification` metadata for both baseline and candidate artifacts in text and JSON output.
- Comparison summaries now explicitly show verification-status transitions and whether verification metadata changed, alongside the existing added/removed/changed staged-record counts.
- Added focused test coverage for verification metadata in comparison payloads and reviewer-facing comparison summaries.
- Verified `node --test scripts/exhibit-ingest/compare-staging.test.mjs`; broader backend verification is rerun after regenerating the fixture-backed David Zwirner staging artifact.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled staging run plus artifact comparison before the source can be marked `verified_live`.

## 2026-06-22 David Zwirner Verification Status Slice

- Added `summary.verification` to staged exhibition reports so each source run now records whether it is `verified_live`, `pending_live_verification`, or still `unknown`.
- Updated `scripts/exhibit-ingest/review-staging.mjs` to print the verification state and notes ahead of the grouped review sections, and to include the same metadata in JSON review payloads.
- Annotated the New Museum source config as already live-verified in this workspace and both David Zwirner exhibition source configs as pending live verification until a network-enabled comparison run is completed.
- Regenerated `data/staging/david-zwirner-exhibitions.json` so the default David Zwirner review artifact now carries explicit fixture-backed verification status.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, and `node scripts/exhibit-ingest/review-staging.mjs --staging data/staging/david-zwirner-exhibitions.json --json`.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled staging run plus artifact comparison before the source can be marked `verified_live`.

## 2026-06-22 David Zwirner Live Verification Prep Slice

- Added `--output` support to `scripts/exhibit-ingest/ingest.mjs` so a live source verification run can write to a separate staging file instead of overwriting the default fixture-backed review artifact.
- Added `scripts/exhibit-ingest/compare-staging.mjs`, a staging-only comparison CLI that validates two staging reports and summarizes added, removed, and changed staged records.
- Added focused tests for custom ingest output paths and live-vs-fixture staging comparisons, including a machine-readable `--json` comparison mode for downstream review tooling.
- Added `npm run compare:staging` and documented the David Zwirner live verification flow: stage to `data/staging/david-zwirner-exhibitions.live.json`, then compare against the fixture-backed report.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, `npm run review:staging:david-zwirner:json`, and `npm run compare:staging -- --baseline data/staging/david-zwirner-exhibitions.json --candidate data/staging/david-zwirner-exhibitions.json`.
- Remaining David Zwirner gap: a network-enabled live staging run still needs to be executed and compared against the fixture-backed artifact.

## 2026-06-22 David Zwirner Review JSON Slice

- Added `--json` support to `scripts/exhibit-ingest/review-staging.mjs` so the staged reviewer queue can be exported as grouped machine-readable JSON alongside the existing text summary.
- Added focused test coverage for the JSON review payload shape to keep downstream reviewer tooling stable as staging sections evolve.
- Added source-specific `npm run review:staging:david-zwirner:json` and `npm run review:staging:new-museum:json` commands plus documentation updates for the staging review flow.
- Verified the David Zwirner fixture-backed staging report can now be reviewed in both text and JSON forms without mutating review statuses or canonical JSON.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled verification run against the current parser and fixture sample.

## 2026-06-22 David Zwirner Source Audit Trail Slice

- Added `summary.sourcePages` to staged exhibition reports so reviewers can see the exact official source URLs used for a run and whether each page was fetched live or from a checked-in fixture.
- Updated the reviewer summary CLI to print source page audit metadata before the grouped `create`, `update`, `possibleDuplicate`, and `conflict` sections.
- Added test coverage for live and fixture-backed source page metadata in staging reports and reviewer summaries.
- Regenerated `data/staging/david-zwirner-exhibitions.json` so the David Zwirner review artifact now records its fixture-backed source page explicitly.
- Verified `npm test`, `npm run build`, `npm run ingest:exhibits:david-zwirner:stage`, `npm run review:staging:david-zwirner:summary`, and `npm run review:staging:david-zwirner`.
- Noted an automation caveat: source-specific staging and review commands should run sequentially, not in parallel, because they share the same staging file.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled verification run against the current parser and fixture sample.

## 2026-06-22 David Zwirner Reviewer Summary Slice

- Added `scripts/exhibit-ingest/review-staging.mjs`, a staging-only review CLI that validates a staging report and prints grouped `create`, `update`, `possibleDuplicate`, and `conflict` sections for human audit.
- Added `npm run review:staging:david-zwirner:summary` and `npm run review:staging:new-museum:summary` so each official exhibition source now has a source-specific review summary command in addition to the approval dry run.
- Added focused test coverage for the review summary formatting to keep the reviewer-facing output stable as staging fields evolve.
- Verified the David Zwirner fixture-backed staging report can now be reviewed in a concise text summary without changing staged statuses or canonical JSON.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled verification run against the current parser and fixture sample.

## 2026-06-22 David Zwirner Approval CLI Verification Slice

- Added temp-file CLI tests for `scripts/exhibit-ingest/approve-staging.mjs` covering both dry-run and `--apply` modes.
- Verified the approval script reports promotable approved creates without mutating canonical JSON during dry runs.
- Verified the approval script writes approved exhibition creates into a caller-specified canonical file only when `--apply` is present.
- Re-ran the David Zwirner fixture-backed staging flow and review dry run to confirm the exhibition source still remains staging-only.
- Remaining David Zwirner gap: the live official exhibitions page still needs a network-enabled verification run against the current parser and fixture sample.

## 2026-06-22 David Zwirner Exhibition Source Slice

- Added a dedicated `david-zwirner-exhibitions` parser that stages NYC exhibition listings from the official David Zwirner exhibitions index page and ignores non-NYC listings.
- Added local fixture page support in `ingest.mjs` so source configs can point at checked-in HTML for offline verification without weakening the review-first staging workflow.
- Added official live and fixture-backed David Zwirner source configs plus package scripts for staging and dry-run review.
- Added fixture-based tests for the David Zwirner exhibition parser and file-backed source loading.
- Generated `data/staging/david-zwirner-exhibitions.json` through the staging pipeline and kept all items pending for human review.
- Remaining gap: the live David Zwirner staging command still needs a network-enabled verification run against the official page.

## 2026-06-22 Owner Manual

- Added `BACKEND_OWNER_MANUAL.md` as the human-facing control document for the exhibition backend.
- Documented what the backend is right now, what data exists, what ingestion does, where staged data goes, what canonical data means, what Codex can safely change, what requires human approval, the next milestone, and how to verify alignment.

## 2026-06-22 Backend Dedupe, Validation, and Approval Slice

- Added conservative exhibition dedupe scoring for staged candidates using normalized source/exhibition URL, normalized title, normalized venue, start date/month, and city.
- Staging reports now classify likely or possible exhibition matches as `possibleDuplicate` proposals instead of plain creates, while preserving human review as the required merge step.
- Added JSON Schema validation for staging reports during ingestion and in tests using AJV 2020-12 support.
- Added a dry-run-first staging approval script for approved exhibition create proposals. It validates canonical exhibition records before promotion and does not promote pending items or possible duplicates.
- Documented `data/exhibit-records.json` as the temporary canonical JSON store and `data/staging` as the validated human-review queue.
- Added `npm test`, `npm run ingest:exhibits:new-museum:stage`, and `npm run review:staging:new-museum` coverage for the backend workflow.
- Verification passed with 12 backend tests, production build, live New Museum staging, legacy David Zwirner staging, and New Museum approval dry run.
- `npm install --save-dev ajv ajv-formats` reported 5 existing npm audit findings in the dependency tree. No automatic audit fix was run because that may change broader Vite/React tooling behavior.

## 2026-06-22 Backend Checkpoint

- Repository files are currently untracked in git, so this note is the audit checkpoint before schema, staging test, and exhibition adapter work.
- Existing canonical data file: `data/exhibit-records.json` contains an empty `records` array.
- Existing staging flow: `npm run ingest:exhibits:stage` writes a David Zwirner artist/artwork staging report to `data/staging/margot-nielsen-2026-06-16-david-zwirner-artists.json`.
- Current direction: preserve staging as the human-review queue and add exhibition-specific schema/tests before expanding source coverage.
